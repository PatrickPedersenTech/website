{
  "name": "patrickpedersen-portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio — Patrick Pedersen",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "wrangler": "^4.95.0"
  }
}
